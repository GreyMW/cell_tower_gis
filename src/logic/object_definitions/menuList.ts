enum MenuList {
    main,
    location_of_interest,
    add_location_of_interest,
    add_points,
    add_delete_layer,
    layer_menu,
    annotation_menu,
    layer_options
}
export default MenuList;
import MapStateInterface from "../../../logic/object_definitions/mapStateInterface.ts";

export default function AnnotationsMenu({mapState}:{mapState: MapStateInterface}){
    return(
        <div>
            <p>Add annotations menu</p>
        </div>
    )
}